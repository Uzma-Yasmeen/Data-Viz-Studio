/* ---------------------------------------------------------------
   The only file you need to edit after deploying to Render.
   --------------------------------------------------------------- */
window.DATAVIZ_CONFIG = {
  // Your Render service URL, no trailing slash.
  appUrl: "https://data-viz-studio.onrender.com",

  // Shown in the footer. Leave blank to keep the default link.
  repoUrl: "https://github.com/Uzma-Yasmeen/Data-Viz-Studio",

  // Stop waiting and hand the visitor over to Render anyway (ms).
  maxWaitMs: 90000,

  // Roughly how long a free Render instance takes to boot. Used only to
  // pace the progress bar, never to decide that the app is actually up.
  expectedColdStartMs: 55000,
};
